import { StagePlaceholder } from '@/components/stage-placeholder'

export default function ServicePage() {
  return (
    <StagePlaceholder
      title="Service Due"
      stage={4}
      description="Tanks due for service with overdue alerts, upcoming schedule, and quick scheduling actions."
    />
  )
}
