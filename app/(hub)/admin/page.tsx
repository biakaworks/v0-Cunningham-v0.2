import { StagePlaceholder } from '@/components/stage-placeholder'

export default function AdminPage() {
  return (
    <StagePlaceholder
      title="Admin"
      stage={7}
      description="System administration with user management, role permissions, checklist templates, and audit logs."
    />
  )
}
