import { StagePlaceholder } from '@/components/stage-placeholder'

export default function CustomersPage() {
  return (
    <StagePlaceholder
      title="Customers"
      stage={2}
      description="Customer list with search, filtering, and quick access to sites and service history."
    />
  )
}
