import { StagePlaceholder } from '@/components/stage-placeholder'

export default function InspectionsPage() {
  return (
    <StagePlaceholder
      title="Inspections"
      stage={4}
      description="Inspection queue with scheduled visits, in-progress work, and completed inspections awaiting reports."
    />
  )
}
