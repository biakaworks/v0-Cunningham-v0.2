import { StagePlaceholder } from '@/components/stage-placeholder'

export default function DashboardPage() {
  return (
    <StagePlaceholder
      title="Dashboard"
      stage={2}
      description="Leadership overview with pipeline value, map preview, active projects, service due items, and attention alerts."
    />
  )
}
